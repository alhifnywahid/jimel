{
  "name": "@jimel/api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute tempmail --file=./schema.sql",
    "db:init:local": "wrangler d1 execute tempmail --local --file=./schema.sql",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@jimel/shared": "*",
    "hono": "^4.6.14",
    "postal-mime": "^2.4.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260811.1",
    "typescript": "^7.0.2",
    "wrangler": "^4.40.0"
  }
}
